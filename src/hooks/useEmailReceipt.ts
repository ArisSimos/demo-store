// This file is now a placeholder since Gmail/Nodemailer is used on the backend.
// You can safely remove this file if not imported anywhere.

export const useEmailReceipt = () => {
  return {
    sendReceiptEmail: async () => false,
    isSendingEmail: false,
  };
};
